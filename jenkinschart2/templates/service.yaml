apiVersion: v1
kind: Service
metadata:
  name: {{ include "jenkinschart2.fullname" . }}
  labels:
    {{- include "jenkinschart2.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "jenkinschart2.selectorLabels" . | nindent 4 }}
