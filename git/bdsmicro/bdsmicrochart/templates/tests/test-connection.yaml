apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bdsmicrochart.fullname" . }}-test-connection"
  labels:
    {{- include "bdsmicrochart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bdsmicrochart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
