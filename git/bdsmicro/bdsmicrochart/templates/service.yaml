apiVersion: v1
kind: Service
metadata:
  name: {{ include "bdsmicrochart.fullname" . }}
  labels:
    {{- include "bdsmicrochart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bdsmicrochart.selectorLabels" . | nindent 4 }}
